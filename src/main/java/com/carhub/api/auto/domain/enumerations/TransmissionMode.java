package com.carhub.api.auto.domain.enumerations;

public enum TransmissionMode {
    MT, //Manual Transmission (MT).
    AT, //Automated Transmission (AT).
    SAT, //Semi-Automated Transmission (SAT).
    CVT, //Continuously Variable Transmission (CVT).
    DCT, //Dual-Clutch Transmission (DCT).
    DSG //DSG (Direct Shift Gearbox).
}
